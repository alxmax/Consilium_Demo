.claude/
__pycache__/
*.pyc
FEEDBACK.html
runs/
files/
# report.html embeds the oracle ANSWER values (analyze.py output) — never commit.
# results_presentation.html is the human-readable report and is intentionally tracked
# (it shows scores only, never the answer key).
report.html
# Answer keys must NEVER be tracked in this repo — they live in the sibling
# Benchmark-scoring/ repo. If this directory reappears here, move it back
# to ../Benchmark-scoring/ before committing.
scoring/
