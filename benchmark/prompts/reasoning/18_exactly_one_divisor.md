# Task 18 — Divisible by exactly one

**Type:** Reasoning | **No code required**

---

Consider the integers from **1 to 100,000 inclusive**.

How many of them are divisible by **exactly one** of the three numbers **6, 10, 15** — that is, divisible by exactly one of those three, and not by either of the other two?

- A) 26,666
- B) 23,333
- C) 16,666
- D) Something else

## Required answer format

**Pick exactly ONE option — A, B, C, or D — and justify it.**

Your response must follow this exact structure:

1. **First line** (nothing else on it): `ANSWER: A` (or B, C, D — one letter only).
2. **Second line** (nothing else on it): `VALUE: <number>` — the exact count, as an integer (format example only: `VALUE: XXXXX`).
3. **Then the justification**, covering:
   - The counts divisible by 6, by 10, and by 15.
   - The pairwise and triple least-common-multiples and the counts divisible by them — and any simplification they produce.
   - The arithmetic that isolates "divisible by exactly one" (not "at least one").
   - Why each of the other three options is wrong (one sentence each).

No hedging. No "it depends". No picking two. Commit to one letter.

**Also save the same response to a file `answer.md` in your assigned workspace folder.** The runner reads that file to extract the `ANSWER:` line and compare it to the expected answer. If `answer.md` is missing or its first non-empty line is not `ANSWER: <letter>`, the correctness check fails automatically.
